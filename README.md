A Apple Pie — Historical digital narrative

This small static site demonstrates a reusable gallery component that allows clicking through 15 images on each page.

Files:
- css/styles.css — basic styling
- index.html
- js/gallery.js — gallery logic (prev/next, keyboard)
- images

Note: The gallery displays a single large image on each page (no thumbnail strip). Use the prev/next buttons or left/right arrow keys to navigate the 15 images.

Note: When the page is not active with users over than 1 minute, a pop screen will show and ask "Are you still there?" This function is to make sure the content will jump back to the content warning initial page automatically, so the next visitor in the gallery will not be confused where to start viewing our digital content.
